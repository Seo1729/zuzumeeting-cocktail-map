# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 로컬 환경변수 (카카오 JS 키)
.env
.env.local

# Claude Code 로컬 설정 (개발자마다 다름)
.claude/settings.local.json

# 디자인 캔버스 산출물.
# design/*.dc.html과 canvas.json이 원본이고, 이 파일은 거기서 다시 만들어진다.
# 2.5MB짜리 에디터가 통째로 박혀 있어 저장소에 둘 이유가 없다.
design/*.html
!design/*.dc.html
